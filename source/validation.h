#pragma once
#include <string>

bool isPasswordValid(std::string pass); //returns a boolean if the password is valid
bool isInputValid(std::string input); //returns a boolean if the input is valid
